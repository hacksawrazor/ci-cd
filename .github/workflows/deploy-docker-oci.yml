name: Reusable Dynamic Docker Deploy to OCI

on:
  workflow_call:
    inputs:
      image-name:
        description: 'Image name (e.g. org/my-api)'
        required: true
        type: string
      deploy-path:
        description: 'Target path on the OCI server'
        required: true
        type: string
      use-sudo:
        description: 'Use passwordless sudo to prepare paths under /opt or other privileged directories'
        required: false
        type: boolean
        default: true
      container-port:
        description: 'Fallback port mapping if no docker-compose.yml exists'
        required: false
        type: string
        default: '8000:8000'
    secrets:
      SSH_HOST:
        required: true
      SSH_USER:
        required: true
      SSH_KEY:
        required: true
      SSH_PORT:
        required: false
      # Key-value JSON containing individually mapped secrets
      INDIVIDUAL_SECRETS_JSON:
        required: false

permissions:
  contents: read
  packages: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Build and Push Docker Image
        uses: hacksawrazor/ci-cd/.github/actions/build-docker-image@main
        with:
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
          image-name: ${{ inputs.image-name }}
          image-tag: ${{ github.sha }}

      - name: Prepare Docker Deployment
        uses: hacksawrazor/ci-cd/.github/actions/prepare-docker-deployment@main
        with:
          full-image: ghcr.io/${{ inputs.image-name }}
          container-port: ${{ inputs.container-port }}
          secrets-json: ${{ secrets.INDIVIDUAL_SECRETS_JSON || '{}' }}

      - name: Deploy Container via Docker Compose on OCI
        uses: hacksawrazor/ci-cd/.github/actions/deploy-docker-compose@main
        with:
          host: ${{ secrets.SSH_HOST }}
          user: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.SSH_PORT || '22' }}
          deploy-path: ${{ inputs.deploy-path }}
          use-sudo: ${{ inputs.use-sudo }}
          full-image: ghcr.io/${{ inputs.image-name }}
          image-tag: ${{ github.sha }}
          registry: ghcr.io
          registry-username: ${{ github.actor }}
          registry-token: ${{ secrets.GITHUB_TOKEN }}